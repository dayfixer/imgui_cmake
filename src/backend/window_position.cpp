#include "window_position.h"

constexpr int s_x = 100;
constexpr int s_y = 100;
constexpr int s_w = 1280;
constexpr int s_h = 720;

WindowPosition::WindowPosition() : x(s_x), y(s_y), width(s_w), height(s_h) {
}
